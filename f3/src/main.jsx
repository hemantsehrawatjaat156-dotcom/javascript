style from "./style.css";
import App from "./app.jsx";
import { createRoot } from "react-dom/client";

on Menu() {
    nsole.log(addition(2, 3));
    rn9der(<App />, document.getElementById("root"));

    
}